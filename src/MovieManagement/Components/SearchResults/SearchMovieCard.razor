@inject NavigationManager NavigationManager

<img src="@Movie.PosterUrl" alt="Image not found" @onclick="@(() => ShowMovieDetails(Movie.Id))" />
<h4>@Movie.Title</h4>
@if (Movie.ReleaseDate != null)
{
    <p>Release date: @Movie.ReleaseDate</p>
}
